version: '3.8'

services:
  datasieve-ai:
    build: .
    container_name: datasieve-ai
    ports:
      - "8502:8502"
      - "8001:8001"
    volumes:
      - ./data_exports:/app/data_exports
    restart: unless-stopped
